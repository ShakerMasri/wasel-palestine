version: "3.8"
services:
  db:
    image: postgres:15
    container_name: wasel_db
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: wasel_palestine
    ports:
      - "5432:5432"
